# ===================================================================
# Project Terminus: Standard Android .gitignore
# ===================================================================

# Built application files
*.apk
*.aab

# Files for the ART/Dalvik VM
*.dex

# Built C/C++ libraries
/app/build/intermediates/cmake/debug/obj/
/app/build/intermediates/cmake/release/obj/

# Android Studio Build Folders
# These contain all compiled code, resources, and intermediate files.
# They are regenerated on every build.
/build/
/app/build/

# Local configuration file (VERY IMPORTANT)
# This file contains the path to your Android SDK and other local settings.
# It should NEVER be committed to version control.
local.properties

# IDE-specific files (Android Studio / IntelliJ)
# We ignore the entire .idea directory, but explicitly keep files
# that define project structure and settings, which are safe to share.
.idea/caches/
.idea/libraries/
.idea/modules.xml
.idea/workspace.xml
.idea/navEditor.xml
.idea/assetWizardSettings.xml
!/.idea/misc.xml
!/.idea/gradle.xml
!/.idea/vcs.xml

# Gradle files
# These are local caches and wrappers that are specific to your machine.
.gradle/
captures/
build-cache/
*.class

# Misc
.DS_Store
